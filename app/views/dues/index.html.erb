<br />

<div class="booyah-box col-10 offset-1">

  <% @dues.each do |due| %>
    <h2 class="text-center"><%= due.title %></h2>
    <div>
      <%= link_to 'More Details', due_path(due), class: 'btn btn-primary btn-large' %>
    </div>
    <br class="clr" />
  <% end %>
</div>